# Universal Scanner

Native Android barcode and QR scanner. Always-on camera, live scan log, and APK build via GitHub Actions.

## Build

The repository uses AndroidX, Jetpack Compose, CameraX, and ML Kit barcode scanning. The `main` branch automatically builds a debug APK through GitHub Actions.

CI verification branch.
